export const pagingOptions = {
    skip: 0,
    take: 20,
    query: ''
};

export type Paging = {
    skip: number,
    take: number,
    query: string,
};

export const THEME = {
    DARK: 'dark',
    LIGHT: 'light'
};